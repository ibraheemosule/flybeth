import express from "express";
import cors from "cors";
import helmet from "helmet";
import { config as dotenvConfig } from "dotenv";
import winston from "winston";
import { createProxyMiddleware } from "http-proxy-middleware";
import rateLimit from "express-rate-limit";
export { express, cors, helmet, dotenvConfig, winston, createProxyMiddleware, rateLimit, };
export type { Express, Request, Response, NextFunction } from "express";
export type { CorsOptions } from "cors";
export { RedisClient, SessionManager, CacheManager, RateLimiter, } from "./redis";
export { HttpClient, createHttpClient, createServiceHttpClient, type HttpClientConfig, } from "./http-client";
export declare const createCorsMiddleware: (options?: any) => (req: cors.CorsRequest, res: {
    statusCode?: number | undefined;
    setHeader(key: string, value: string): any;
    end(): any;
}, next: (err?: any) => any) => void;
export declare const createHelmetMiddleware: () => (req: import("http").IncomingMessage, res: import("http").ServerResponse, next: (err?: unknown) => void) => void;
export declare const createLogger: (serviceName: string) => winston.Logger;
export { hashPassword, comparePassword, generateApiKey, generateBookingReference, AuthUtils, type JWTPayload, type AuthConfig, } from "./auth";
